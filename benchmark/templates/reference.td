<div id="container">
  <h3 class="{header}">Exists</h3>
  <p>This is a template with a tornado exists body with {ref} and another not {exists} body {references} all {over} the {place} with a child exists {body}</p>
</div>
